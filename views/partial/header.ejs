<md-toolbar id="nav" class="md-whiteframe-5dp">
    <div class="md-toolbar-tools">
        <h2 class="md-flex">Nebulae</h2>
        <span flex></span>

        <a ng-if="!isAuthenticated" href="#/login">Login</a>
        <a ng-if="!isAuthenticated" href="#/signup">Signup</a>
        <a ng-if="isAuthenticated" href="#/admin">Admin</a>
        <a ng-if="isAuthenticated" href="#/dashboard">Dashboard</a>


        <md-menu ng-if="isAuthenticated">
            <md-button ng-click="$mdOpenMenu($event)" class="md-icon-button" aria-label="{{sessionUser.email}}">
                <img ng-src="http://gravatar.com/avatar/{{sessionUser.gravatar}}"
                     class="md-avatar"
                     alt="{{sessionUser.email}}" />
            </md-button>
            <md-menu-content>
                <md-menu-item><md-button ng-href="/auth/logout">Logout</md-button></md-menu-item>
            </md-menu-content>
        </md-menu>




    </div>
</md-toolbar>